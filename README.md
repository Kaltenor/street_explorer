# Street Explorer

Street Explorer is an Expo React Native app focused on recording real-world exploration on foot.

The app records GPS paths locally, stores them in SQLite, and displays explored areas as deduplicated 15m x 15m cells on a map.

## Run

```bash
npm install
npx expo start
```

Current local project path:

```text
W:\street_explorer
```

Use the development build for real device testing, especially background location. Expo Go is only useful for quick foreground checks.

## Docs

- [Project Overview](docs/PROJECT_OVERVIEW.md)
- [Architecture](docs/ARCHITECTURE.md)
- [Roadmap](docs/ROADMAP.md)
- [Testing](docs/TESTING.md)
- [Development Build](docs/DEVELOPMENT_BUILD.md)
- [Downloadable Medal Country Packs](docs/COUNTRY_PACKS.md)
- [Changelog](docs/CHANGELOG.md)
- [Historical Medal System Design Record](docs/MEDAL_SYSTEM_IMPLEMENTATION_PLAN.md)

## Current Status

Current version: `v0.27.2`

Explorer Score awards one point for every unique 15m tile discovered, one additional point for every enclosed tile, and 200 points for every completed daily expedition. It is reconstructed retroactively from local exploration data and permanent expedition seals, updates live during a walk, appears with a source breakdown in Details, and adds earned points to enclosure and expedition reward stamps. When idle, the Field Log presents both Explorer Points and today's steps; while recording, it retains distance, duration, and current-walk steps. Start and Stop share the same compact centered control, with GPS status immediately below it. The seven-destination Atlas dock occupies one fixed safe-area footer coordinate throughout the map, main pages, and secondary pages, supports direct page switching, and includes an explicit Map destination with its localized name visible while the map is selected. Returning from another tab restores the enlarged map title until the next map touch. During recording, a coordinated HUD transition hides that title, lifts the medal/objective stack, and settles the Field Log against the dock to maximize the visible playfield. Details also turns mapped surface into a 55-step offline international comparison ladder, from a tennis court to Qatar.

The app uses the lightweight 1320x2868 `loading-screen3.jpg` for its shared native splash and in-app launch presentation. The React presentation mounts above the database, font, and map startup gates, so hidden preparation cannot postpone the subtitle or Press to start. One launch-wide clock gives the clean artwork a one-second budget across the native-to-React handoff instead of restarting that delay after the in-app image decodes. The overlay then performs a roughly one-second native UI-thread cyan Walk → gold Explore → parchment Reveal sequence, waits another half-second, and fades in a pulsing Press to start prompt. Preparation feedback remains silent until the player presses: a ready press immediately begins an 800ms splash-to-map fade, while an unready press replaces the prompt with the localized loading row and automatically starts the same fade when readiness completes. A half-size version label stays independently in the safe bottom-right corner. Reduce Motion preserves the requested timing and fade while publishing subtitle and pulse states without spatial motion. It also supports self-healing foreground/background recording, accuracy-aware startup and one-time walking-scale recording-start centering, a persistent four-direction hand-inked cartographer with native geographic anchoring, game-owned location presentation, and camera-independent panning, hole-free full-walk live rendering, bounded durable late-GPS recovery, walking-focused GPS filtering, full-screen Atlas-styled Details/History/Completion/Medals views, explored cells, loop-fill analysis, landmark medal collection, safe backup/restore, device step counts, city-wide district outlines with a closure- and Stop-driven map objective HUD, and persistent OpenStreetMap street completion derived from frozen saved routes.

Options includes persistent Appearance and Feedback controls. Explorator preserves the established dark navy/gold atlas, Daylight switches every styled app surface, the native map, and status-bar foreground to a warm high-contrast light scheme for outdoor visibility, and the former non-functional Custom placeholder has been removed. Sound effects and haptics are separately persisted, enabled by default, and can be disabled without affecting navigation or reward completion.

Midnight Cartographer gives the iOS playfield the same identity as the navy/gold interface: Apple MapKit uses a dark muted presentation, generic POIs and the native blue cursor are hidden, and only game-owned landmarks, route markers, and the player remain. Translucent burnt orange means explored territory, gold marks today's discoveries and the active walk, parchment highlights a selected saved route, inferred links remain bright teal, and other saved walks use restrained atlas colors. Every district is rendered exactly once with a quiet copper outline that strengthens only when selected; the containing city uses a restrained wine perimeter instead of error red and strengthens only for a city objective. The selected district or city receives a quiet parchment wash that stays distinct from burnt-orange explored territory. Administrative overlays are gated to the selected objective city, and cross-city switches remount the native MapKit surface at its last camera region so a GPS-derived or retained previous-city perimeter cannot survive; explored territory stays visible. Start, finish, and medal locations use custom hand-inked parchment markers instead of native pins.

Atlas Cabinet unifies Map, Details, History, Completion, Expeditions, Medals, Options, recording details, and diagnostics with the same dynamic bottom menu, plus the established Atlas page headers and content transitions. `AtlasNavigationDockLayer` pins the thicker 56-point dock to identical safe-area coordinates on Map and every page; the dock itself no longer participates in page entrance or map-return animation, so switching changes only its selected destination while content moves behind it. Map has a dedicated map icon, another icon switches pages directly, and the selected destination expands to its localized name. The map keeps its Field Log and recording actions immediately above the dock. Inactive map destinations still support a deliberate localized-title preview without opening. The four persistent map surfaces form separate, lightly inset Atlas stripes with consistent 7px screen gutters and restrained 10px corners: a combined medal-and-flag rail, the objective ledger, the walking field log, and navigation. They use the same low-opacity paper grain, parchment/Cinzel identity text, restrained gold rules, and engraved selection states while keeping live values in the system face; transient scope controls retain the same visual language. Gold is reserved for selection, progress, rewards, and primary actions. Stop confirmation and the recording summary reuse the same textured dialog language while keeping the destructive action red. On iOS, every Atlas page also supports an interactive left-edge swipe back: the complete page follows the finger over the live map, short drags spring back, and committed distance or velocity completes the existing Back action. A recording-detail swipe returns to History before a second swipe returns to the map. District/city selection uses a compact hand-inked cartographer seal dynamically centered in the map space between the measured top and bottom controls. Its fitted title/detail typography stays inside the quiet navy center, and the shared strike remains fully hidden until that message's artwork is loaded so image and lettering become visible on the exact same animation frame. Completion reuses the same generated gold/navy/burnt-orange artwork at the restrained standard scale, while a tight white ink halo and navy drop shadow keep its dynamic labels readable. Closing a new explored enclosure and completing an expedition reuse the full map-centered location-change presentation with event-specific wording and medium haptics. App-lifetime players make the stamp impact audible on the award frame, then start the locally bundled CC0 victory jingle 90ms later. All game feedback mixes over external music and podcasts instead of pausing them; ordinary location stamps retain only their quiet ink sound. New explored cells flash as fresh orange ink, and a focused saved route draws onto the map before its finish marker appears.

Recording Recovery V2 opens every authoritative unfinished session as a full-screen saved-route preview after draining pending background batches. It verifies the native GPS task, reports Active, Interrupted, or Uncertain status, recommends the safest next action without removing Resume/Finish/Discard, and requires an editable date/time-based name that is committed atomically when a recovered walk is finished.

The medal catalogue keeps metropolitan France's 100 communes and 875 reviewed landmarks bundled for immediate offline use, including Lyon's 44-medal album with four new outer-district landmarks in each of arrondissements 3, 4, 6, 7, 8, and 9, then adds 449 albums and 3,850 medals through checksum-pinned downloadable country packs for Belgium, Germany, Italy, the Netherlands, and Spain. A landmark is collectible whenever its 15 m anchor tile was directly discovered or lies anywhere inside the same validated, area-capped surface rendered as explored on the map. Each launch automatically checks every bundled album plus every already-installed country pack against that complete surface, awards matches across all cities, and queues their normal celebrations without downloading missing packs. Launch readiness waits for this local-only award pass; a failed pass is cleared for retry, and a changed exploration revision is checked again. Newly discovered active-city tiles and newly enclosed surface interiors can award live without requiring the medal anchor itself to be walked. Every unlock uses an original two-second stereo orchestral cue with rising brass, strings, timpani, and bell shimmer; it is generated in-repository without third-party samples or melodies and dedicated to CC0/public domain. The Medals page browses the active album through All and every available city subdivision, using complete loaded district boundaries rather than only districts named by medal metadata; its All Cities scope groups every locally unlocked medal by city without loading unvisited catalogues. Locked map markers reveal only their landmark name and localized Locked mention; only collected medal markers open the medal page. The Netherlands pilot measured official-source quality, manually curated Amsterdam, Rotterdam, and Maastricht, and expanded to 58 municipalities and 50.26% population coverage; the 391-city expansion wave occupies only 286,643 compressed bytes, well below its 1 MB budget. Official heritage sources are preferred and every Wikidata/OpenStreetMap fallback is measured explicitly. Selecting a supported city—or one of its parented districts—loads only that album. A first selection downloads and validates that country's versioned gzip once, retains it for offline reuse, and exposes a non-blocking retry state if the network or published artifact is unavailable. Failed requests remain latched until explicit retry, identical saved-data refreshes coalesce, and download/seeding begins only after core History, exploration, objective, recording, and durable-cache hydration. Historical recording scans remain spatial and incremental, while expedition medal checks use bounded indexed queries. The complete coverage, size-gate, source, and publishing contract is in [Downloadable Medal Country Packs](docs/COUNTRY_PACKS.md).

The original hand-inked cartographer uses distinct north/east/south/west idle, three-frame walking, and desaturated stale-GPS poses inside one persistent 64-by-64-point marker. The navy coat, gold trim, parchment hood, and red scarf match the Atlas interface, while a permanent dark compass halo keeps the icon legible over parchment selection washes and burnt-orange explored surfaces and in the stale-GPS state. All frames remain pre-mounted and opacity-selected at the proven 170ms cadence, so MapKit anchoring, Stop/Start continuity, and the accessible last-known-location state remain stable.

The interface uses one consistent navy/gold atlas system with textured lightly inset map stripes, engraved active controls, and reduced default density. The wordmark starts enlarged on first launch and after every return from another Atlas tab, then contracts after the next map interaction. An active walk animates it completely away while the top HUD and Field Log close toward their respective screen edges; stopping restores the compact title, and Reduce Motion publishes either layout immediately. The fixed 56-point icon dock uses 48-point-high, 40-point-wide destinations at regular widths and a responsive compact treatment below 360 points, with invisible hit slop preserving effective 44-point touch areas. Map, Details, History, Completion, Expeditions, Medals, and Options are permanent destinations, and the selected destination expands its localized label on every page. Inactive map destinations retain their deliberate hold preview, and the shared dock remains reachable on secondary pages. A persistent active-city medal-progress rail opens the collection and contains its objective flag at the right edge; map layers live in Options, Completion shows only its four primary measures, and recording diagnostics stay behind explicit technical-detail controls.

Zone Boundary Completion V2 requests complete OSM member geometry for local administrative relations and every district inside the containing city, assembles unordered or reversed way fragments, preserves multiple outer components and holes, and keeps genuinely incomplete geometry display-only. A level-9 relation belongs to a city only when deterministic interior samples from every outer component fall inside that city; shared-edge delegated communes and detached foreign components are excluded even when an old cached parent ID says otherwise. Each cold launch uses the initial foreground GPS fix to replace the saved objective with the containing official level-9 district, falling back to the containing city where no playable district applies; denied or unavailable location and failed boundary lookup preserve the saved objective. The selected objective city keeps its districts outlined, while ordinary panning leaves the objective unchanged. Long-pressing another district inside the current or selected city switches directly to that district. The District/City scope chooser appears only when the held point belongs to a different city than the current boundary context or selected city. Superseded lookups and percentage scans cannot overwrite the newest selection. Exact cached boundaries cannot be downgraded by an incomplete response, and a saved objective is restored after its boundary cache returns. Exact zone denominators are cached against the boundary geometry fingerprint. Completion refreshes boundaries automatically after 30 days, shows refresh status and the last successful date, and records permanent district/city achievements that survive later boundary edits. Backup V5 preserves those achievements alongside routes and medals.
The boundary cache now preserves each relation's original OSM administrative level. Level 9 is the minimum and final playable district tier; level 10 remains stored only for source fidelity and backup compatibility and is excluded from map outlines, objectives, completion, refresh counts, and visible achievement rollups. This policy applies by default across France and other places using the same level-8 city, level-9 district, level-10 neighborhood model. Upgraded legacy caches refresh automatically; a former level-10 objective is cleared while its historical achievement row remains internal.

District Expeditions draw five deterministic offline choices from a 25-archetype catalogue for the selected official district each local day. The district-and-date-seeded shuffle changes at local midnight and excludes street or medal orders when enough unfinished local opportunities do not exist. Its cell missions cover fresh territory, frontier adjacency, gap sealing, dense clusters, district sectors, compass directions, boundaries, the district heart, and outer reaches; street, loop, medal, paired, and staged combination orders provide broader goals. Any combination can be active at once, including missions retained from previous days or other districts; selections persist across relaunch and continue from durable post-acceptance evidence. Every completion awards a permanent journal seal plus 200 Explorer Points without rankings or prescribed routing. A permanent compass destination beside Details, History, Completion, and Medals opens the Atlas expedition journal and shows the same engraved selected state; without a district objective it explains the requirement and hands off to Completion. The objective HUD summarizes one or several active district missions and remains a progress shortcut. Accepting or restarting is disabled during a recording so the evidence boundary remains exact. Backup V5 preserves choices, concurrent active progress, finalized loop evidence, all 25 kind identifiers, and seals, so expedition points restore retroactively from those unique seals.


Street-Aware Path Inference V3 performs a small shared-cache topology fetch only around suspicious GPS gaps. Topology requests use bounded failover between two globally covered public Overpass instances when the primary returns a retryable overload, timeout, network, or response error. Its graph accepts exact OSM joins, verified ground-level geometric crossings, and compatible endpoint seams no wider than 8m while respecting bridge, tunnel, layer, access, and foot metadata. Every accepted frozen bridge stores its confidence, topology reason, snap distances, route metrics, and inferred-cell count; History shows a concise summary with full evidence under Technical details. Existing historical snapshots remain immutable unless the user explicitly runs either full-history Reprocess recordings or History > Reprocess this walk. The individual action refreshes only that recording before reconciling shared totals from every other unchanged frozen route; if that selected calculation fails, it preserves the frozen route and reports failure rather than success.

Saved-route focus now selects and enables the requested History recording in one action. The Today path scope uses recording overlap, so a walk that crosses midnight remains visible on both affected dates.

Performance work introduced in v0.6.6 keeps the player and active route immediate while coalescing expensive exploration contours at most every 650ms without allowing continuous GPS fixes to starve the surface update. The v0.22.3 pass stabilizes native-map callbacks and style/filter props, memoizes administrative, route-marker, medal-marker, player, and explored-surface subtrees, disables continuous native snapshot tracking for static medal markers, and reuses the live enclosure result when deriving Explorer Score. The v0.22.4 pass isolates every route polyline behind stable point references so a long active recording regenerates coordinates only for its changing tail, lazily derives the one-time initial map region, batches raw-fix publication with persisted-walk state, and reads finalized History point totals from frozen route snapshots with an exact indexed fallback for missing or pending snapshots. Native polygon identities still include their full geometry, so closing an enclosure replaces any stale MapKit hole immediately during a walk or after Stop. Saved route queries remain scoped to the visible period or selected recording, History virtualizes its rows, hidden full-screen panels unmount, completion aggregates use indexed SQLite queries, startup no longer waits for the background outbox before mounting, and Backup V5 compresses bounded records without constructing one database-sized JSON value.

History keeps full GPS/route data lazy until a recording is opened, Completion calculations yield and cancel during navigation, and Backup V5 exports visible finalized recordings as a compressed, checksum-verified `.streetexplorer` archive. The newest 20 walks stay in individual hot records; older walks share bounded monthly archive blocks without merging their logical identities. Confirmed frozen-route points reference the single GPS stream, inferred bridge geometry remains lossless, and restore reads and inserts one bounded block at a time. Backup, Export all GPX, restore inspection, and Restore are single-flight operations: History immediately shows an in-progress panel, disables duplicate taps, and keeps that feedback visible through file/database processing. Restore fully verifies the selected archive and previews its export date, source version, size, walk/GPS totals, medals, permanent zone achievements, and expedition seals before confirmation, then revalidates that same file before replacement. Backup still requires verification of the externally saved Files copy. Bulk GPX export streams one portable, uniquely named GPX entry per finalized walk into a ZIP. Restore accepts V5 only; the temporary V4 converter has been removed.
