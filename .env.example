# Copy to .env.local for local Android native builds.
# Also set this in the matching EAS environment for cloud Android builds.
GOOGLE_MAPS_API_KEY=
# Separate key restricted to Maps SDK for iOS and the iOS bundle identifier.
GOOGLE_MAPS_IOS_API_KEY=
