# Project Overview

## Android medal visibility — 0.35.2

Android medal markers now track their custom view until layout plus a bounded 500 ms settling interval, explicitly redraw, then stop tracking. The icon font is preloaded before map startup. This addresses the empty first-snapshot failure without continuously refreshing static medals. Existing zoom/layer visibility and medal collection rules are unchanged. Physical Android confirmation is pending.

## Android fresh-install recovery — 0.35.1

Version 0.35.1 / build 231 includes Android Google Explorator/Daylight styling from 0.35.0, a non-blocking first-location boundary lookup when no objective is cached, identified Overpass requests with a second-server fallback, and a visible retry explanation after failed map holds. Partial/error boundary responses are rejected. Cached areas remain selectable offline. The user confirmed Android map styling and local-area lookup work on 2026-09-22; medal-marker rendering is tracked separately in 0.35.2.

Street Explorer is a mobile exploration app built with Expo, React Native, TypeScript, and local SQLite storage.

The app records real-world movement and turns it into an exploration map. The goal is not to be a sport tracker. The goal is to feel like a real-life exploration game where streets and areas become visible as they are explored.

## Current Activity

Version 0.35.0 adds an iOS Apple Maps / Google Maps choice under Options. Apple is the default; the choice persists. Switching is idle-only, preserves map center/zoom and game data, and is unavailable during recording/recovery/finalization. Older iOS binaries without the Google native module remain on Apple Maps; Android stays Google-only. The iOS development client 0.35.0 / 230 built successfully on 2026-09-22; see Development Build for installation and Testing for the pending physical-device checks.

Android distribution preparation (0.34.8): the Maps SDK key is configured in the EAS preview environment and restricted to the Android package and EAS signing certificate. Google Cloud setup was verified on 2026-09-22. The Android preview APK (build 229) built successfully on 2026-09-22. Real Android rendering/background recording checks remain pending; this is not yet a device-validated Android release.

Street Explorer is dedicated to on-foot exploration. Recordings, paths, statistics, history, completion, and GPS quality rules all use the walking profile. Existing data from older activity profiles is preserved and consolidated into walking history during database migration.

## Current Features

- Foreground GPS recording with automatic watcher recovery.
- Permission-aware background tracking with serialized ownership, atomically published delivered batches, nullable headless owner recovery through a unique timestamp match, bounded unmatched retention, and chunked backlog backpressure.
- Local SQLite persistence.
- A locally bundled CC0 book-page cue at a restrained 50% player volume for entering any Atlas icon-bar destination and returning from it to the map, audible in iPhone Silent mode and mixed over external audio.
- Saved paths displayed on the map, with History focus automatically selecting/enabling the requested route and Today retaining recordings that overlap midnight.
- Active recording path displayed live.
- 15m x 15m deduplicated explored cells.
- A deterministic, retroactive Explorer Score awarding one point per unique non-forbidden discovered tile and one additional point per non-forbidden enclosed tile. Exact Forbidden Zone cell snapshots are an explicit exclusion set across walked, loop-fill, live enclosure, discovered-area, surface-area, and point totals, including defensive overlap from later exploration or restore. It updates live, appears prominently with a source breakdown in Details and beside today's steps in the idle Field Log, and annotates enclosure stamps with the exact points earned. During recording, Field Log retains distance, duration, and current-walk steps. Start and Stop use the same compact centered control with GPS status immediately beneath it. The same Details card compares total mapped surface against a bundled 55-item international ladder, from regulated sports surfaces through public squares, parks, islands, lakes, cities, and countries.
- Gameplay-first closed-loop area fill based on enclosed explored cells, with the full map-centered location-change stamp presentation, localized reward wording, revealed-cell count, immediate preloaded ink impact, and closely following victory jingle when a new enclosure closes during an active walk.
- Idle-only Forbidden Zone selection from the Field Log for completely surrounded inaccessible regions above the shared 150,000 m² automatic loop limit and at or below the configurable 2 km² safety ceiling. Accepted snapshots render as merged translucent-purple polygons that ignore normal long-press interaction, remain separate from explored/loop-fill and Explorer Score cells, remove only their exact administrative overlap from completion denominators, and support confirmed hold-to-remove. Removal explicitly releases its cell snapshot so the same enclosure can be recreated without a false overlap. Each zone also supports an optional local single-line 120-character comment entered after creation or edited later: a normal tap reveals it directly above the tapped purple position for the lifetime of the active district objective. Geometry, cells, and comments persist through reprocessing and boundary refresh and round-trip through optional backward-compatible Backup V5 data.
- Shared authoritative exploration-surface and completion contours, with burnt orange for cells inside exact cached city boundaries, pale yellow for countryside cells outside them, and non-decreasing reprocess safety.
- Visible phased progress and explicit failure reporting for historical reprocessing.
- Full-history or selected-recording OSM corridor repair with retryable-error failover across two globally covered public Overpass instances; the single-walk action replaces only its frozen route before safely reconciling complete exploration, loop-fill, and street totals from unchanged snapshots, and a selected calculation failure remains a visible preserved-route failure rather than a success.
- Ready-gated transition from the launch loader to the map through a discreet `Press here to start` control on every launch.
- Non-blocking GPS, route-cache, step-counter, and background-service initialization during launch and recording startup.
- Map-first startup with accuracy-aware current-location centering, one explicitly sized 64-by-64-point original hand-inked cartographer annotation anchored directly to its MapKit coordinate, pre-mounted four-direction idle, three-frame walking, and stale-GPS artwork, one-time Start/Resume recentering, camera-independent panning, game-owned iOS location presentation, durable last-trustworthy-position continuity across GPS, Stop/Start, force-close/relaunch, and recovery transitions, an accessible stale-fix state, a self-healing idle/recording watcher, cached explored cells, and lazy saved-route loading.
- Midnight Cartographer presentation on iOS with dark muted Apple Maps or custom-styled Google Maps, hidden native POIs and location cursor, burnt-orange explored territory, gold current activity, restrained saved-route colors, and custom parchment-and-ink route/medal markers.
- Persistent app-wide appearance selection in Options: Explorator retains Midnight Cartographer, while Daylight applies a warm high-contrast light palette, light native map, and matching status-bar foreground across every UI surface. The former non-functional Custom placeholder is no longer exposed.
- Separately persisted sound-effect and haptic controls, enabled by default and applied immediately to Atlas navigation, objective stamps, enclosure rewards, map selection, and medal celebrations.
- Medal landmarks retain their independent map visibility through a 0.14 latitude span, twice the former far-level cutoff, without extending heavier route or endpoint overlays.
- Original hand-inked cartographer player with four directional idle poses, three restrained walking frames per direction, desaturated stale-GPS variants, one stable MapKit sprite annotation with overlap-handed-off preloaded layers, and a persistent contrast halo across district surfaces. Automatic localized parchment speech uses a separate same-coordinate, non-selected annotation so native callout fitting cannot move the camera. Its continuously tracked 244-by-128 marker keeps the original panel and arrow locked together as one rigid unit. An Apple Maps-native -58-point center offset places the arrow's lower tip at the top of the hat without the panel covering the sprite, while Google Maps on iOS and Android uses its normalized bottom anchor. Child-only transparency prevents upper-left reactivation snapshots, and elapsed-time character progress catches up after busy background callbacks. Isolated single-marker components prevent speech state from re-rendering the player. Varied witty-adventurer messages react to walk lifecycle, distance, new ground, exploration streaks, revisits, stationary pauses, GPS trouble, and frequent randomized cheers with prioritized one-message queuing, short auto-dismissal, and a Reduce Motion fallback.
- Atlas Cabinet presentation across Map, Details, History, Completion, Medals, Options, recording details, and diagnostics with interactive iOS left-edge swipe back over the live map, nested History-detail unwinding, bundled Cinzel display titles, neutral-edged navy paper panels, gold reserved for active and reward states, quiet page/ink sounds, Reduce Motion-aware transitions, Atlas-textured Stop and recording-summary dialogs, and one fixed safe-area-aware dynamic menu. The same 56-point footer-layer contract keeps all six destinations at identical coordinates, gives Map a dedicated icon and localized selected label, and switches directly between destinations without moving or fading the dock; only content transitions behind it. Returning from a tab enlarges the map title until the next map touch. During a walk, one coordinated transition hides the title, raises the medal/objective stack, and lowers the Field Log to the dock edge while leaving all tabs usable. A 320ms hold on an inactive map destination previews its localized title without navigating, keeps it for 650ms after release, and fades it back over 220ms. Protected History data operations temporarily disable footer navigation. Matching low-grain, lightly inset map stripes retain restrained corners, parchment/Cinzel identity text, an integrated medal-stripe objective action, engraved active controls, neutral GPS framing, and compact 44-point-safe spacing; an original transparent hand-inked cartographer seal adds fitted outlined localized wording, a load-gated reward strike whose artwork and text appear in exact synchronization, restrained completion stamps, explored-area ink reveal, and saved-route draw-on focus.
- Full-walk live/recovery routes retain raw SQLite observations, rebuild deterministically when a fix arrives late, use canonical contiguous indexes, and render stable bounded chunks while only the recent 300 points remain in diagnostic state.
- Confirmed Stop teardown drains entered handlers and the durable background outbox before single-recording finalization. The summary, History row, stats, live cells, and Start control return at that durable boundary; route inference, exact steps, medal/objective checks, and full refresh then reconcile asynchronously through the pending repair outbox; continuous routes without suspicious gaps skip street-corridor graph inference. Underfilled recordings retain a hidden five-minute recovery tombstone, and late finalized merges trigger an immediate safe map refresh. Backup V5 rejects the authoritative active recording, omits hidden recovery tombstones and invisible orphan unfinished rows with their dependent data, reads one transaction, writes compressed checksum records through Expo's current cache-file API, and verifies the externally saved Files copy; import closes both journal and in-memory GPS admission before replacing data.
- Performance-bounded map rendering: immediate player/route updates, non-starving 650ms coalesced exploration snapshots, retry-safe medal analysis, stable native-map callbacks/styles/filters, memoized administrative/surface/route-marker/medal-marker/player/per-polyline subtrees, tail-only long-route coordinate refresh, lazy initial-region derivation, batched persisted GPS state, static medal snapshots, reused live-enclosure score geometry, localized duration ticking, three-second idle tail synchronization, and development render/timing diagnostics.
- Large-history scaling through virtualized History rows, snapshot-backed summary point counts with an exact pending-repair fallback, unmounted hidden panels, Paths queries scoped to the selected display period, indexed exploration/session reads, pre-aggregated completion SQL, and bounded Backup V5 compression/restoration blocks.
- Startup mounts the map after database and language readiness while the coalesced background outbox drain continues concurrently behind the authoritative recovery check.
- Walking-focused GPS quality filters.
- Route history with rename, delete, highlight, GPX export, and confirmed per-recording reprocessing that is disabled during active recording.
- Automatic active-recording recovery that drains durable background data, restores the same SQLite session and original clock into the normal active HUD, recalculates trusted distance from persisted confirmed segments, and restarts one foreground watcher plus permitted background tracking before Start is available. Recovery V2 remains the full-screen Active/Interrupted/Uncertain fallback with Resume/Finish/Discard and atomic naming when automatic recovery is unsafe.
- Device step counts for walking recordings.
- Icon layer controls for paths, cells, and markers.
- Full-screen Details, History, and Completion views with responsive map back navigation, lazy per-recording History details, and cancellable chunked Completion scans.
- Completion screen with scope and zone selectors.
- OpenStreetMap Street Completion V2 rebuilt from immutable saved routes, with nearest direction-compatible metre coverage, repeat-walk deduplication, a 90% OSM-way completion threshold, durable first-completion timestamps/V1 migration evidence, and asynchronous active-walk isolation.
- OSM boundary loading with full local relation-member geometry, robust multi-ring assembly, display-only invalid fallbacks, exact-cache downgrade protection, geometry-fingerprinted denominator caches, saved-objective recovery after cache repopulation, automatic 30-day refresh status, and a visible last-fetched date.
- Permanent exact-boundary completion achievements with district and city rollups that remain earned across later OSM changes and cache clearing.
- Zone-specific completion stats and map focus.
- Objective-gated city boundary context with strict all-component interior containment excluding shared-edge or detached foreign level-9 relations, every valid district rendered exactly once in muted copper with a stronger selected stroke, only one parchment selected-zone wash, and one restrained wine city perimeter that strengthens for a city objective. Cold launch quietly replaces the saved objective with the initial foreground GPS fix's already-cached official district, or its cached city when no playable district applies, while missing cached boundaries preserve the saved fallback without a launch-blocking network request. Cross-city changes hide the previous context, commit an overlay-free frame, and remount MapKit at the preserved camera region so retained native polygons cannot survive without removing globally explored territory. Intentional haptic long-press selection supports direct same-city district switching and a District/City chooser reserved for cross-city holds; a hold inside an exact country but outside all city/district bounds instead clears the objective and publishes localized countryside status, while a hold with no containing country (open ocean) is ignored. Map panning remains objective-preserving, boundary/percentage calculations are latest-only, and the compact objective HUD keeps persisted completion stable during active walking while showing completion, remaining cells, and today's added cells.
- Persisted OSM administrative hierarchy keeps official level-9 districts as the minimum and final playable tier while retaining level-10 neighborhoods internally only for source fidelity and backup compatibility; neighborhoods are excluded from outlines, objectives, completion, refresh counts, and visible achievement rollups, and legacy caches reclassify automatically.
- Objective scope changes use stale-while-revalidate memory and SQLite snapshots keyed by mode, zone, exploration revision, and geometry fingerprint. Launch hydrates the saved objective plus cached containing city/district pair and cached denominators; only the selected missing zone is calculated later, equivalent work is single-flighted, and full scans are deferred throughout active recording/finalization. Permanent achievements always present 100% and zero remaining cells even when newer raw diagnostics are pending or lower after a denominator change.
- Explicit SDK 54 native splash configuration using the 1320x2868 `mapbound-native-splash.png` through `expo-splash-screen`, paired with the visually matching lightweight `loading-screen3.jpg` root-owned React presentation. The deprecated native path and former Street Explorer assets are absent. The React overlay mounts before database, font, and map preparation can gate the UI. A launch-epoch clock budgets one clean-background second across the native-to-React handoff, so a slow startup does not add a second post-decode wait. The in-app layer then reveals three real cyan/gold/parchment English/French Walk/Explore/Reveal phrase nodes over 1050ms, waits half a second, and fades in a pulsing Press to start. Loading feedback is intentionally post-press: an already-ready map begins its slow fade immediately, while unfinished preparation shows the loading row on the retained splash and fades automatically when ready. The shared dismissal flag suppresses completion-stamp creation and all Atlas stamp rendering, sound, and haptics until the fade has finished, while preserving eligible cached rewards for post-launch display. An independent half-size version remains in the safe bottom-right corner. Reduce Motion keeps the sequence but publishes moving elements at their final states; the transparent `title.png` supplies the matching map logo overlay.
- A frozen 6,082-medal offline France pack for the 500 largest communes, including departments and collectivities overseas, with a 20-medal minimum for ranks 1–100 and 10 for ranks 101–500. Curated Paris, Marseille, Lyon, Villeurbanne, and Thonon-les-Bains identities remain stable. Another 449 albums and 3,850 medals live in versioned downloadable Belgium, Germany, Italy, Netherlands, and Spain packs whose compact city-zone/checksum manifest remains bundled. First use verifies and atomically caches one country's gzip for offline reuse. The Netherlands pilot manually curates Amsterdam, Rotterdam, and Maastricht; all packs prefer official population and heritage sources with explicitly measured fallback and limitations. Reproducible distribution and SQLite measurements live in `country-packs/v1/medal-storage-report.json`.
- Reward-only landmark reading: an unlocked medal description expands into a read-only in-app Wikipedia frame in the selected app language, with alternate-language, search, and default-browser fallbacks; locked medals reveal no article action.
- Validated-surface medal collection awards any landmark whose 15 m anchor tile was directly discovered or lies inside an enclosed cell accepted by the same 150,000m2-capped geometry rendered as explored. One automatic launch operation reconstructs that surface, checks every bundled city and every already-installed country pack without downloading absent packs, seeds matching albums sequentially, queues normal presentations, and exposes the results through All Cities before launch readiness. The scan is keyed to the persisted exploration revision and failed work is cleared for retry. Active-city direct-tile and combined saved/current-surface collection can occur live; the separate single-recording enclosure path retains its 80 m minimum, one-cell seam tolerance, accepted finalized route geometry, and strict interior anchors. Unsupported cities expose no fallback album. An unavailable country payload shows a non-blocking retry state without delaying History, exploration, objectives, recording hydration, or launch discovery awards from available albums; the failed download request stays latched until explicit retry, and identical saved-data refreshes are coalesced.
- Medal pins, a persistent city medal-progress card on the map, and a full-screen collection with an active-city/All Cities scope, All plus every boundary-backed city subdivision for the active album (including zero-medal districts), grouped offline-safe unlocked medals across visited cities, permanent Unlocked/Locked sections, Unicode-safe localized landmark copy, unclipped chips, richer earned descriptions, and landmark focus. Locked map pins expose only their name and localized Locked callout; collected pins alone open Medals.
- Recoverable medal presentation with an original two-second stereo CC0/public-domain orchestral cue, haptic success cue, reduced-motion support, a 3D reveal, localized description, Continue-triggered flight into the Medal tab, and an acknowledged queue. The in-repository cue combines rising brass, strings, timpani, and bell shimmer without third-party samples or melodies. The global feedback session mixes every game sound over external music and podcasts instead of interrupting playback.
- User-confirmed recording-history scans remain spatial and incremental with per-album catalogue-version cursors, and unchanged scans process zero walks. The automatic launch pass checks definitions against the compact persisted discovered-cell set without loading saved GPS routes; downloadable definitions participate only when their country pack is already installed.
- A streamlined navy/gold presentation system across the map HUD, walk controls, full-screen menus, summaries, recovery, and diagnostics. The medal, objective, navigation, and field-log controls form four separate stripes with consistent 7px screen gutters and restrained 10px corners using the same low-opacity paper grain, parchment/Cinzel identity typography, restrained gold ornaments, and engraved active states as the Atlas pages. The city medal rail uses a close 46-point frame; the fixed navigation rail uses a 56-point frame with 48-point-high, 40-point-wide regular controls, responsive narrow-screen widths, and hit slop preserving effective 44-point targets. The objective flag remains integrated at the medal stripe's right edge without stacking a second texture. Live data remains in the system face, the GPS frame stays neutral while only its dot/label carries status color, and advanced tools and evidence remain in Options or expandable technical details.
- Backup V5 preserves individual session identity, raw GPS points, frozen route snapshots, inferred bridge evidence, medal state, permanent zone achievements, and optional exact Forbidden Zone geometry/cell snapshots with comments. Older V5 manifests without Forbidden Zones remain importable and restore empty Forbidden Zone state; v0.29 zone entries without comments restore with no label text. The newest 20 walks remain individual hot records; older walks are physically consolidated into bounded monthly blocks without fake merged recordings. Restore accepts V5 only, fully verifies and previews archive identity/totals before confirmation, then revalidates the same file before replacement. Bulk GPX export streams one file per finalized walk into a shared ZIP; the temporary V4 converter has been removed.

## Current Limitations

- Background tracking is configured but may not fully work in Expo Go on iOS. A development build is likely required for real locked-screen recording.
- Explored cells are grid-based, not exact street geometry.
- OSM is no longer a primary visible gameplay overlay by default.
- District boundaries depend on what exists in OSM near the current location.
- Very large zone denominators may be skipped to keep the app responsive.
- There is no backend, account, cloud sync, social feature, or route suggestion system.
- OpenStreetMap streets remain hidden analysis data; cells and recorded paths are still the primary exploration map.
- Street Completion V2 reports cached walked/loaded OSM distance, not recording distance or a full city-wide denominator. Accuracy still depends on locally cached OSM geometry and tagging; new cache coverage enters the denominator on the next asynchronous rebuild.
- Only high- and medium-confidence topology-validated street bridges contribute inferred explored cells. V3 accepts exact joins, ground-level geometric crossings, grade-compatible endpoint seams up to 8m, and bounded jointly evaluated nearby snaps while excluding `foot=use_sidepath` decoys. It persists per-bridge evidence/cell attribution and leaves unmatched or unsafe gaps hidden with no straight-line fallback.

## Audit follow-up (0.34.4)

The resilience pass adds atomic migration-ledger updates, body-aware network deadlines, retryable core startup loading, historical street completion dates, per-record backup yielding, per-session street rebuilding, and incremental live boundaries. The resilience pass preserves the V5 format; the subsequent expedition removal retires its seals. Large restored histories still need physical-iPhone memory/frame-time validation; see [Audit follow-up](AUDIT_FOLLOWUP.md).

Expeditions are retired as of 0.34.4. Existing scores lose the former 200 points per seal; remaining exploration and collectible progress persists. Legacy V5 archives remain usable and their expedition payload is ignored.

Version 0.34.6 gives city/district and countryside switches a discreet unboxed place label, soft native animation, and the user-selected piano sound. Medal unlocks, objective completion, and enclosure rewards keep their established celebrations. The label respects sound preferences, Reduce Motion, both appearances, and rapid replacement.

Version 0.34.7 removes avoidable waits from city/district holds: visible-boundary selection, cached city-only selection without network, deferred completion reads, and cancellation of superseded requests. The Completion page provides the existing tap-based objective picker.
